# Secrets & local config -- never commit these
config.yaml
.env
*.key
*.pem            # Kalshi RSA private keys -- NEVER commit these
kalshi_*.pem

# Cached market data
data/cache/*.parquet
data/cache/*.csv

# Paper-bot runtime state (bankroll, positions, logs)
data/paper_bot/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/
