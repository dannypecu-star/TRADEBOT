pandas>=2.0
numpy>=1.24
ccxt>=4.0
pyyaml>=6.0
pyarrow>=12.0
pytest>=7.0
requests>=2.28    # Kalshi API client + The Odds API + platform webhooks (3Commas/Cryptohopper)
cryptography>=41  # Kalshi RSA-PSS request signing

# --- Optional, platform-specific (install only if you use that platform) ---
# MetaTrader5           # drive an MT5 terminal from Python (Windows only)
#
# Monitoring uses only the Python standard library (http.server) -- no extra deps.
# Prometheus + Grafana run as containers via deploy/docker-compose.yml.
